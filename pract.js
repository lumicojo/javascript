const first = 3;
const second = 2;
const problem = 55;
const luckyNumber = first < second ? (problem + 2) : problem / 3;
console.log(luckyNumber);

const name = "lum";
const lastName = "mini";
const fullName = name && lastName;
console.log({name});

let favoriteNumber = 32;
if (favoriteNumber >= 10) {
    favoriteNumber = favoriteNumber + 10
}
console.log(favoriteNumber);

let number = 32;
if (number < 10) { 
    number = number + 10
}
console.log(number);

let numberB = 20;
while (numberB < 5) {
    numberB = numberB +2
}
console.log(numberB);

