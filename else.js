/*
const age = 14;

let isAdult;

if (age >= 18) {
  isAdult = true;
} else {
  isAdult = false;
}

console.log(isAdult);
*/
//ternary Eexpression
const age = 14;

let isAdult;

age >= 18 ? (isAdult = true) : (isAdult = false);

console.log(isAdult);

